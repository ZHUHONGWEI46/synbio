# Generated cache (not distributed)

`run_all.py` creates `features/esm2_650m/` on first use.  Each mean-pooled
embedding is stored under the SHA-256 hash of its sequence.  This directory is
excluded from version control and from `A.zip`.
