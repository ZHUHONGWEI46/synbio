# Results

本目录用于保存团队可复用的结果产物。

| 目录 | 用途 |
| --- | --- |
| `figures/` | Wiki、答辩、报告可复用图 |
| `tables/` | 关键结果表、统计汇总 |
| `models/` | 模型权重索引和校验信息 |
| `reports/` | 阶段性结果报告 |

约定：

- 每个关键图表应能对应到生成脚本、输入数据和配置。
- 大模型权重不直接提交；请在 `models/README.md` 记录下载位置、版本和 SHA-256。
- 文件名建议包含日期、实验或任务编号和简短描述。
